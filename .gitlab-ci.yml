# GitLab CI/CD pipeline for lightweight Python validation

stages:
  - test

# Python validation job
test:
  stage: test
  image: python:3.9
  variables:
    # Do not keep pip cache on runner disks.
    PIP_NO_CACHE_DIR: "1"
    PIP_DISABLE_PIP_VERSION_CHECK: "1"
  before_script:
    - pip install --upgrade pip
    # Install only CI dependencies (avoids ML/GPU packages like torch/CUDA).
    - pip install -r requirements-ci.txt
  script:
    - python --version
    # Repository-wide syntax/import bytecode compilation check.
    - python -m compileall .
    # Keep scope lightweight and stable for CI.
    - pytest -q rag_backend/tests
  allow_failure: false

default:
  interruptible: true
